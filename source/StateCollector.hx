import haxe.macro.Context;
import haxe.macro.Expr;

class StateCollector {
    // public static macro function collectFlxStates():Expr {
    //     var flxStateClasses = Context.getType("flixel.FlxState").getSubTypes();
    //     var exprs = flxStateClasses.map(function(t) {
    //         return macro $v{t};
    //     });
    //     return macro [ $a{exprs} ];
    // }
}